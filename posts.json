---
layout: none
permalink: /posts.json
---
{
  "posts": [
    {% for post in site.data.slot %}
      {
        "title": "{{ post.title | escape }}",
        "url": "{{ post.url }}",
        "img": "{{ post.img }}",
        "rtp": "{{ post.rtp }}",
        "date": "{{ post.date | date: '%Y-%m-%d' }}",
        "excerpt": "{{ post.desc }}",
        "content": "{{ post.content | strip_html | strip_newlines }}"
      }{% unless forloop.last %},{% endunless %}
    {% endfor %}
  ]
}